class polynomial():
    def __init__(self, identifier, mod=None):
        if type(identifier[0]) == tuple:
            # This is so cool!
        elif type(identifier[0]) == int or type(identifier[0]) == float:
            # I am using MacVim!!!
	    # This is interesting!!!
        else:
            raise TypeError("Invalid Input")
